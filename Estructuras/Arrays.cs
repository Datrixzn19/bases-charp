/*
Tamaño fijo: Si creas un array de 5 espacios, siempre tendrá 5 espacios.
Acceso rápido: Es muy eficiente para la memoria.
Base cero: El primer elemento siempre es el [0].
*/



// Declaramos un array de 3 enteros



/*
int[] edades = new int[3]; 

edades[0] = 20;
edades[1] = 25;
edades[2] = 30;

// También se puede inicializar directamente
string[] nombres = { "Ana", "Luis", "Juan" };




/*
Los arrays son más limitados. De hecho, un array por sí solo no tiene métodos como "Borrar" o "Agregar", porque su tamaño no puede cambiar.

.Length: Te dice el tamaño total del array (lo que definiste al crearlo).

Array.Sort(miArray): (Método estático) Ordena los elementos de menor a mayor.

Array.Reverse(miArray): Invierte el orden del array.

Array.IndexOf(miArray, valor): Te dice en qué posición está un elemento. Si no lo encuentra, devuelve -1.

Dato clave: Para usar métodos avanzados en Arrays como Sort o IndexOf, normalmente usamos la clase Array antes del nombre del método, pasándole nuestro array como argumento.
*/